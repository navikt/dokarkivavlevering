name: Deploy dokarkivavlevering [dev-fss]
on:
  workflow_dispatch:
    inputs:
      periodeStart:
        description: 'Periode start (f.o.m) for avlevering. YYYY-MM-DD'
        required: true
      periodeSlutt:
        description: 'Periode slutt (t.o.m) for avlevering. YYYY-MM-DD'
        required: true
      tema:
        description: 'Tema for avlevering'
        required: true

env:
  IMAGE: docker.pkg.github.com/${{ github.repository }}/dokarkivavlevering:${{ github.sha }}

jobs:
  build:
    permissions:
      contents: read
      packages: write
    name: Checkout code and build docker image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2-
      - name: Build and run tests
        run: mvn -B package --file pom.xml
      - name: Build Docker image
        run: docker build . --pull --tag ${IMAGE}
      - name: Push Docker image
        run: |
          docker login --username "$GITHUB_REPOSITORY" -p ${{ secrets.GITHUB_TOKEN }} https://docker.pkg.github.com
          docker push ${IMAGE}
  deploy:
    name: Deploy job to dev
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy job to NAIS dev-fss
        uses: nais/deploy/actions/deploy@v1
        env:
          PRINT_PAYLOAD: true
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-fss
          RESOURCE: nais/naiserator.yaml
          VAR: periodestart=${{github.event.inputs.periodeStart}},periodeslutt=${{github.event.inputs.periodeSlutt}},tema=${{github.event.inputs.tema}}
          VARS: nais/dev-config.json
          WAIT: true
          TIMEOUT: 1h